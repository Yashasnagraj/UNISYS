cmake_minimum_required(VERSION 3.16)

include($ENV{IDF_PATH}/tools/cmake/project.cmake)

project(resoscan_firmware
        VERSION 0.1.0
        DESCRIPTION "ResoScan ESP32 firmware — RMS bone resonance pipeline")
